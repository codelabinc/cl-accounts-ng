import { AccountDataSource } from './account-data-source';

describe('AccountDataSource', () => {
  it('should create an instance', () => {
    expect(new AccountDataSource()).toBeTruthy();
  });
});
